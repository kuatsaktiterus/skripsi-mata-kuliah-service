BEGIN TRY

BEGIN TRAN;

-- DropIndex
ALTER TABLE [dbo].[MataKuliah] DROP CONSTRAINT [MataKuliah_semester_jurusanId_key];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
